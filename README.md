# CN_Sessional
Design and implement a system(using C) where clients are dynamically categorized as either Reader or Writer, based on user input through an HTML form. The system must manage concurrent access to a shared database (representing the chat data), using synchronization techniques inspired by the Readers–Writers problem to ensure data consistency.
